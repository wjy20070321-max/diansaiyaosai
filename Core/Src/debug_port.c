#include "debug_port.h"   // 调试端口模块头文件
#include <string.h>       // 标准字符串处理库（当前文件里暂未实际使用，可保留）

/**
 * @brief 调试信息打印函数
 * @param s 要输出的字符串指针
 *
 * @note 当前这个函数是一个“空实现”，也可以理解为一个预留接口。
 *       现在调用 DebugPort_Print() 不会真正输出任何内容。
 *
 *       这样设计的好处是：
 *       上层代码可以先统一调用这个接口，
 *       以后如果你想把调试信息输出到某个硬件端口，
 *       只需要改这一个函数，不用全工程到处改。
 *
 *       后续你可以把它重定向到：
 *       - USART 串口
 *       - 串口屏
 *       - USB 虚拟串口
 *       - 其他调试接口
 *
 *       例如，如果你以后想重定向到某个串口，
 *       就可以在这里加入 HAL_UART_Transmit()。
 */
void DebugPort_Print(const char *s)
{
    (void)s;  // 显式忽略参数，避免编译器因“未使用参数”而报警告

    /* 当前默认不做任何输出 */
    /* 后续可按需要重定向到 USART1 / USART3 / USART6 等串口 */
}
